# Include Demo - Using the Seq Standard Library
#
# This example demonstrates how to use the include system to import
# functions from the standard library.
#
# Usage:
#   seqc --output include_demo main.seq
#   ./include_demo

include std:math
include std:stack-utils

: main ( -- Int )
  "=== Include Demo ===" io.write-line
  "" io.write-line

  # Using math functions from std:math
  "Math functions:" io.write-line
  "  abs(-42) = " -42 abs int->string string.concat io.write-line
  "  gcd(48, 18) = " 48 18 gcd int->string string.concat io.write-line
  "  min(10, 5) = " 10 5 min int->string string.concat io.write-line
  "  max(10, 5) = " 10 5 max int->string string.concat io.write-line
  "  clamp(150, 0, 100) = " 150 0 100 clamp int->string string.concat io.write-line
  "  square(7) = " 7 square int->string string.concat io.write-line
  "" io.write-line

  # Using stack utilities from std:stack-utils
  "Stack utilities:" io.write-line
  "  2dup of (3, 4): " 3 4 2dup
    # Stack now: ( 3 4 3 4 )
    int->string " " string.concat swap int->string string.concat
    " " string.concat swap int->string string.concat
    " " string.concat swap int->string string.concat
    string.concat io.write-line

  0
;
