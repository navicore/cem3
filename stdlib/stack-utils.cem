# Stack Manipulation Utilities
# Limited to: dup, drop, swap, over, rot, nip, tuck

# Duplicate top two values
: 2dup ( A B -- A B A B )
  over over
;

# Drop top two values
: 2drop ( A B -- )
  drop drop
;

# Triple drop
: 3drop ( A B C -- )
  drop drop drop
;
