# Test file.slurp error handling

# Test reading a file that doesn't exist
: test-not-found ( -- )
  "/nonexistent/path/to/file.txt" file.slurp
  test.assert-not  # should return false for missing file
  drop  # drop empty string
;

# Test reading a file that exists (this file itself!)
: test-exists ( -- )
  "tests/integration/src/test-file-safe.seq" file.exists?
  test.assert
;
