# Test yaml-parse-nested directly

include std:yaml

: main ( -- Int )
  "Test yaml-parse-nested" write_line

  # Test 1: Simple flat parsing
  "host: localhost
port: 8080" yaml-parse-nested

  if
    "Parsed OK: " swap yaml-serialize string-concat write_line
  else
    drop "Parse failed" write_line
  then

  0
;
